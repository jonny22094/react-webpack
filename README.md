# React starter kit 

## Run
| `npm i`
| 2. `npm run watch`
| 3. Server start in `localhost:3000`
  
  
## Starter kits

Branch     | Technologies
---------- | ---------- 
**master** | react + webpack + eslint + flowTypes
**mobx**   | master + mobx-state-tree
**redux**  | master + redux

